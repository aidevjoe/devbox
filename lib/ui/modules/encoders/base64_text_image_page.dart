import 'package:flutter/material.dart';

class Base64TextImagePage extends StatelessWidget {
  const Base64TextImagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
