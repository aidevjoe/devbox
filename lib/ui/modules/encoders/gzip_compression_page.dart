import 'package:flutter/material.dart';

class GzipCompressionPage extends StatelessWidget {
  const GzipCompressionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
