import 'package:flutter/material.dart';

class HtmlFormatterPage extends StatelessWidget {
  const HtmlFormatterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
