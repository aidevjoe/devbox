import 'package:flutter/material.dart';

class ChecksumPage extends StatelessWidget {
  const ChecksumPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
